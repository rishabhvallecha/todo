import React from 'react';

import './react-saga.css';

/* eslint-disable-next-line */
export interface ReactSagaProps {}

export const ReactSaga = (props: ReactSagaProps) => {
  return (
    <div>
      <h1>Welcome to React-saga!</h1>
    </div>
  );
};

export default ReactSaga;
