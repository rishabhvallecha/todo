import React from 'react';

import './react-store.css';

/* eslint-disable-next-line */
export interface ReactStoreProps {}

export const ReactStore = (props: ReactStoreProps) => {
  return (
    <div>
      <h1>Welcome to React-store!</h1>
    </div>
  );
};

export default ReactStore;
