import React from 'react';

import './saga.css';

/* eslint-disable-next-line */
export interface SagaProps {}

export const Saga = (props: SagaProps) => {
  return (
    <div>
      <h1>Welcome to saga!</h1>
    </div>
  );
};

export default Saga;
